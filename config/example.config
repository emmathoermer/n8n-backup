# Example n8n configuration file
# Replace sensitive values with placeholders before committing

# Server configuration
port=5678
protocol=http
host=localhost

# Paths
EXECUTIONS_DATA_DIRECTORY=${N8N_DATA_FOLDER}/executions
WORKFLOW_DATA_DIRECTORY=${N8N_DATA_FOLDER}/workflows

# Security settings (replace with your secure values)
BASIC_AUTH_ACTIVE=true
BASIC_AUTH_USER=<username>
BASIC_AUTH_PASSWORD=<password>

# Database settings (replace with your secure values)
DB_TYPE=sqlite
DB_PATH=<path-to-database>

# Webhook settings
WEBHOOK_URL=<your-webhook-url>

# Note: Replace all <placeholders> with actual values in your local setup
# Do not commit actual sensitive values to this repository 